extends layout

block content
    style.
        .container {
            padding: 25px;
            position: fixed;
        }

        .form-login {
            background-color: #EDEDED;
            padding-top: 10px;
            padding-bottom: 20px;
            padding-left: 20px;
            padding-right: 20px;
            border-radius: 15px;
            border-color: #d2d2d2;
            border-width: 5px;
            box-shadow: 0 1px 0 #cfcfcf;
        }

        h4 {
            border: 0 solid #fff;
            border-bottom-width: 1px;
            padding-bottom: 10px;
            text-align: center;
        }

        .form-control {
            border-radius: 10px;
        }

        .wrapper {
            text-align: center;
        }
    link(href='//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css', rel='stylesheet')
    .container
        .row
            if message
                .alert.alert-danger
                    strong Error:
                    | #{message}
            .col-md-offset-5.col-md-3
                form.form-login(method='POST', action='/users/signin')
                    h4 Welcome back.
                    input#userName.form-control.input-sm.chat-input(type='text', placeholder='username', name='username')
                    br
                    input#userPassword.form-control.input-sm.chat-input(type='password', placeholder='password', name='password')
                    br
                    .wrapper
                        span.group-btn
                            button.btn.btn-primary.btn-md(type='submit')
                                | login
                                i.fa.fa-sign-in
                    br
                    a(href='/users/signup', style='text-align:center') Don't Have an account? Signup!
